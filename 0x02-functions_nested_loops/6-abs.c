#include "main.h"

/**
* _abs - Computes absolute value of the number from zero
* @n: The integer
*
* Return absolute value of the input number
*/

int _abs(int n)
{
	if (n < 0)
		return (-1 * n);
	else
		return (0);
}
